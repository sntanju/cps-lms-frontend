# Base URL of the Strapi backend.
# NEXT_PUBLIC_ because the browser calls the API directly with its access token.
# Local development: http://localhost:1337
# Production (Vercel): the Railway backend URL.
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337
